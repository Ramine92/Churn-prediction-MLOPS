name: CI Pipeline

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

permissions:
    contents: read
jobs:
    test:
        name: Run Tests & Lint
        runs-on: ubuntu-latest

        steps: 
            - name: Checkout Code
              uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98
            
            - name: Set up Python 3.11
              uses: actions/setup-python@0a5c67180f8585816306db5a4b711598ce38290e
              with:
                python-version: "3.11"
                cache: "pip"
            
            - name: Install Dependencies
              run: | 
                python -m pip install --upgrade pip
                pip install -r requirements.txt

            - name: Run Tests With Pytest
              run: pytest tests/